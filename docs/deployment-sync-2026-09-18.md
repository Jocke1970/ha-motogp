# Home Assistant ↔ GitHub deployment inventory — 2026-09-18

**Separate verified/user-reported host events, repository source, and open gaps.** A GitHub commit does not automatically update HA.

| Component | Verified or user-reported status | Remaining work |
| --- | --- | --- |
| Running Python | Exact customized `motogp_sensor` v1.0.9: 14 source files plus inventory committed to `dev` in [`0c66663`](https://github.com/Jocke1970/ha-motogp/commit/0c66663aff1ee7db30de136b064941964791219f); [integrity CI passed](https://github.com/Jocke1970/ha-motogp/actions/runs/35378743131). Nine upstream-identical, five locally modified. | Dated snapshot only; no continuous sync or complete HA runtime/deployment validation. Never run old v1.0.10-only patch against live modified v1.0.9. |
| Dashboard-mode package | `/config/packages/motogp_dashboard_mode.yaml` exported and matched byte-for-byte by [`config/packages/motogp_dashboard_mode.yaml`](../config/packages/motogp_dashboard_mode.yaml) on `dev`; former duplicate quarantined separately. | Do not rerun installer that overwrites YAML. Capture dashboard/helper references. |
| Eleven historical `.sh` scripts | Reviewed 13-file provenance ZIP and matched old feature families with deployed Python. First host scan: 6,618 files, zero references, eight large skips, correctly stopped without movement. Second guarded `--apply` verified snapshot/provenance/script hashes, scanned **6,626 files including all eight large**, zero filename/wildcard references, and user terminal reported **11 scripts moved** into `/config/.motogp_cleanup_quarantine/patch-scripts`. Python, YAML, JS, backups and non-MotoGP scripts untouched. | No permanent deletion. Validate operation after relocation, inspect external cron/add-on/manual callers outside `/config` if configured, check hashes before distinct removal approval. [Executed runbook](patch-quarantine-runbook-2026-09-18.md). |
| Seven old YAML files | Six misplaced Lovelace cards and former duplicate package separately quarantined earlier; user reported no repair warnings. | Audit references before distinct permanent deletion; legacy dashboard untouched. |
| New frontend / Card-test | JS card observed in HA; exported `/config/www/ha-motogp-card.js` differs from `dev/frontend/ha-motogp-card.js` in bytes despite matching `v0.1.0-dev.3`/build metadata. | Exact diff, browser-loaded resource verification and new stamped build before replacing anything. |
| Historical results | Isolated [`backend/result_archive.py`](../backend/result_archive.py) with fake-API tests. | Not installed. Verify actual historical Moto2 FP1 classification, integrate HA Store, spoiler-safe API/UI and selector, test restart/transitions/rollback. |

**Cleanup scope:** eleven named old scripts moved reversibly; do not delete provenance or source ZIP, active package, JS, non-MotoGP scripts or the separate seven YAML files. External jobs outside `/config` remain outside both scans. `beta`, `main` and running Python/JS were not updated by this work. Any future deployment requires tested install and recovery steps.

[Source audit](deployed-python-audit-2026-09-18.md) · [Patch provenance audit](provenance-cleanup-2026-09-18.md) · [Project status](project-status.md) · [Issue #1](https://github.com/Jocke1970/ha-motogp/issues/1)
