# Documentation sync ledger — 2026-09-21

Scope: `Jocke1970/ha-motogp`; documentation-only reconciliation after HACS `v1.0.9.1b1` first operator install. No live HA actions, frontend, Python, manifest, HACS metadata, release tag or YAML changes follow from this ledger.

## Prior state and gaps

- Last broad development audit: 2026-09-18, `dev` only. The old `docs/project-status.md` on `main` and `beta` still instructed installing upstream 1.0.10 and implementing TV delay. `dev` said no archive was installed or promoted; these were stale **after** the 2026-09-20 HACS beta publication and operator HACS install.
- The overlay-only `docs/session-archive-beta1-2026-09-20.md` and earlier roadmaps record decisions/history but must not be presented as the current installation procedure. HACS distribution: tagged `v1.0.9.1b1`, with current instructions at `docs/hacs-beta-installation.md`.
- No post-install full host-source export or live session JSON has been supplied, so installed-version reporting cannot be promoted to end-to-end archive verification.

## Verified reference points

- `dev` pre-beta source and offline tests: `c4939f4a8997e526f5846d306161bccdb9a3d8c7`; original 1.0.9 Python inventory remains the *historical* snapshot.
- HACS packaging CI passed: https://github.com/Jocke1970/ha-motogp/actions/runs/35528926529 . `beta` package commit `a40b9ebe9e5a8cf515e6209f5ff56ebba0dafb5f`; tagged prerelease: https://github.com/Jocke1970/ha-motogp/releases/tag/v1.0.9.1b1 . `main` initial distribution commit `ba28f5e8f5cc594468f4a7740efa9460da8c077e`.
- User HACS report: `installed_version=v1.0.9.1b1`, `auto_update=false`, `in_progress=false`. Prior `latest_version=ba28f5e` and `skipped_version=ba28f5e` were a branch-commit/version-selection discrepancy, not another published beta. After enabling the HACS Pre-release switch for the new repository, UI download/version badge displayed `v1.0.9.1b1`; post-toggle `latest_version` attributes not supplied. Do not declare that value fixed without seeing it.
- HA Jinja Ninja found 36 MotoGP-matching entities with static race/calendar/results/standings data. Offline live sensors were observed after the weekend's final race; user intentionally set delay to 0 s. No remaining live session that weekend. Two HACS update entities seen, which alone is not proof of duplicate running integrations.
- A previous standalone race observation JSONL exists; not an automatically imported session JSON, not retroactive archive evidence. Next UI remains independent (last reported working `0.3.0-dev.4`); legacy dashboard must remain untouched.

## Documentation changes and conventions

1. `docs/project-status.md` on `main`, `beta` and `dev` is the current status/next-milestones reference. Keep identical narrative across branches, without conflating codebases. Each branch README should link to it and the current HACS guide.
2. Preserve original dated audit, roadmap and overlay documents for traceability; mark superseded overlay installation instructions clearly. Do not silently rewrite the 2026-09-18 audit as though it had examined 2026-09-20 installed files.
3. `main` is HACS discovery/distribution, `beta` the beta packaging lane and `dev` independent development. A docs-only commit is not a new runtime release. Only an explicitly approved tagged version changes the installed beta.
4. Distinguish **CI verified** (packaging/archive regressions), **user observed** (HACS version/entitites), **not verified** (host file hashes, real archive files, session transitions/restart recovery), and **future work** (Replay, safe read API, lap expanders, Moto2/Moto3 standings, Next layout).

## Next checkpoint — first race weekend with this backend

- Before a qualifying live session, leave the beta intact; no reinstall, untagged-branch upgrade, old upstream repository, or overlay script.
- During/after the next available session, read-only inspect real `/config/motogp_data` JSON existence and `schema_version`, full event/category/session ID, rider completed laps and coverage; inspect HA logs for warnings. Do not mistake the earlier JSONL for this archive.
- Across session changes verify different files, no loss at finish, and no fabricated laps. Restart recovery and positive TV delay require separately planned trials; current user delay 0 s must not be silently changed. No-spoiler/API exposure is not yet implemented for historical UI.
- Keep dated findings and link them back from `docs/project-status.md`. Audit HACS pre-release `latest_version` metadata separately if still discrepant; keep `auto_update` off while testing.

**Not a doc-sync finding:** A repository release existing or source code containing a hook cannot prove a session was recorded while the HA host was not running or before the package was installed.
